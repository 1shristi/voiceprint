# Optional — restricts which origins can call /analyze. Comma-separated. Leave empty for "*"
ALLOWED_ORIGINS=""

# Optional — shared secret required in the X-API-Key header. Leave empty to disable.
API_KEY=""
